# 랭킹 동점 처리

## 개요

- **시작일**: 2026-04-24
- **상태**: 완료
- **관련 브랜치**: feature/101

## 목표

- [x] 동일한 스탯 값을 가진 캐릭터에게 같은 순위 부여 (standard competition ranking: 1,1,1,4,...)
- [x] 순위 색상 표시(금/은/동)를 index 기반 → rank 기반으로 변경
- [x] `getRankings()` 함수 동점 처리 적용 (O(n) 단일 패스)
- [x] `getCharacterWeaponRank`, `getRankForWeaponCombo` 동점 처리 적용
- [x] main 브랜치 머지 (기간 대비 버그 랭킹 복구)

## 진행 상황

### 2026-04-24

- 동점 처리 로직 구현 완료
- `getRankings()` O(n²) → O(n) 개선
- main 머지로 `BugRankingList` + `bugsPerMonth` 복구
- Firebase 할당량 초과로 빌드 중단 → 오후 5시 이후 재시도 필요

## 실행 스크립트

(스크립트 없음 - 프론트엔드 로직 수정)

## 중단 시 이어서 할 작업

- [ ] Firebase 할당량 회복 후 `npm run build` 재실행하여 최종 확인
- [ ] PR 생성 (feature/101 → develop)

## 관련 파일

- `src/app/stats/page.tsx`
- `src/lib/stats-data.ts`
