# 실험체 기본 스텟 크롤링

## 개요

- **시작일**: 2026-04-17
- **상태**: 완료
- **관련 브랜치**: feature/91

## 목표

- [x] 나무위키에서 실험체 기본 스텟 크롤링 스크립트 작성
- [x] 저장할 스텟: 공격력(기본/성장), 방어력(기본/성장), 체력(기본/성장), 공격속도, 이동속도
- [x] 무기군 복수 보유 실험체 처리 (예: 얀 - 글러브/톤파별 다른 성장치)
- [x] 크롤링 결과 JSON 파일로 저장 및 검증 → **87/87 전원 성공**
- [x] (이후) Firebase에 저장

## 데이터 구조 설계

```typescript
// 무기군별 스텟 (성장치가 무기군마다 다름)
type WeaponStat = {
  attackSpeedGrowth: number; // 공격속도 성장 (%)
  basicAttackAmpGrowth?: number; // 기본 공격 증폭 성장 (%)
  skillAmpGrowth?: number; // 스킬 증폭 성장 (%)
};

// 실험체 기본 스텟
type BaseStats = {
  // 체력
  hpBase: number;
  hpGrowth: number;
  // 방어력
  defenseBase: number;
  defenseGrowth: number;
  // 공격력
  attackBase: number;
  attackGrowth: number;
  // 이동속도
  moveSpeed: number;
  // 공격속도
  attackSpeedBase: number;
  // 무기군별 성장치
  weaponTypes: Record<string, WeaponStat>;
};
```

## 참고 링크

- 윌리엄: https://namu.wiki/w/%EC%9C%8C%EB%A6%AC%EC%97%84(%EC%9D%B4%ED%84%B0%EB%84%90%20%EB%A6%AC%ED%84%B4)#s-3
- 얀: https://namu.wiki/w/%EC%96%80(%EC%9D%B4%ED%84%B0%EB%84%90%20%EB%A6%AC%ED%84%B4)

## 진행 상황

### 2026-04-17

- `scripts/crawl-character-stats.ts` 작성 완료
- 나무위키 HTML 구조 분석: `레벨 1 / 레벨 20 / 성장치` 테이블 구조
- `<details>` 접힌 섹션 자동 펼치기로 무기 숙련도 섹션 파싱
- 무기군 동적 감지 구현 (고정 목록 → 단독 행 패턴 감지)
- `"Lv. 1"` / `"최대 체력"` 등 캐릭터별 다른 표현 처리
- Firestore 저장 `namuWikiUrl` 활용 (접미사 없는 URL 대응)
- **87/87 전원 성공**, 무기군 23종 발견
- 결과: `scripts/character-stats-result.json` 저장
- 커밋: `7b0944c`

### 2026-04-17 (2차)

- `scripts/upload-character-stats.ts` 작성
- `characters/{name}` 문서에 `baseStats` 필드로 저장 (옵션 A)
- `src/types/patch.ts`에 `WeaponStat`, `BaseStats` 타입 추가
- **87/87 Firebase 업로드 완료**

## 실행 스크립트

### 1단계: 스텟 크롤링 (JSON 저장)

```bash
npx tsx scripts/crawl-character-stats.ts
```

### 2단계: 결과 검증 후 Firebase 업로드

```bash
npx tsx scripts/upload-character-stats.ts
```

## 중단 시 이어서 할 작업

없음 (완료)

## 관련 파일

- `scripts/crawl-character-stats.ts` (신규)
- `scripts/upload-character-stats.ts` (신규, 이후)
- `src/types/patch.ts` - 타입 추가 예정
