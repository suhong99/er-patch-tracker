# 클로드 스킬 설정

## 개요

- **시작일**: 2026-03-18
- **상태**: 완료
- **관련 브랜치**: feature/77

## 목표

- [x] 프로젝트에 맞는 Claude Code 스킬(.claude/commands/) 설정
- [x] Forced Eval Hook 설정으로 스킬 자동 활성화
- [x] 스킬 내용을 실제 워크플로우에 맞게 조정

## 진행 상황

### 2026-03-18

- Scott Spence 아티클 참고: Forced Eval Hook(84% 성공률) 방식 채택
- `.claude/commands/` 디렉토리 생성 및 스킬 파일 작성
- `.claude/hooks/skill-eval.sh` + `.claude/settings.json` 으로 훅 등록
- 사용자 피드백 반영:
  - `/feature-branch` 제거 (사용자가 직접 생성)
  - `/pr` 제거 (사용자가 직접 생성)
  - `/crawl` → 파이프라인 실행에서 **파싱 로직 개선 워크플로우**로 변경
    - generate-fixtures → compare-with-firestore → 로직 수정 → npm test 순서

## 생성된 파일

```
.claude/
├── commands/
│   ├── work-log.md    → /work-log  (새 작업 시작 시 로그 생성)
│   ├── crawl.md       → /crawl     (파싱 로직 개선 워크플로우)
│   └── lint-fix.md    → /lint-fix  (ESLint + Prettier)
├── hooks/
│   └── skill-eval.sh  (매 프롬프트마다 스킬 평가 강제)
└── settings.json      (UserPromptSubmit 훅 등록)
```

## 관련 파일

- `.claude/commands/work-log.md`
- `.claude/commands/crawl.md`
- `.claude/commands/lint-fix.md`
- `.claude/hooks/skill-eval.sh`
- `.claude/settings.json`
